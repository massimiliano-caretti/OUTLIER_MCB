"""evals — a deterministic, offline evaluation harness for OUTLIER_MCB.

It compares OUTLIER_MCB against simple baselines on frozen tasks and produces repeatable numeric
metrics. No network, no LLM, no unseeded randomness. See README.md for what it does and does NOT measure.
"""
